import React from "react";
import "./Jumbotron.css";

const Jumbotron = () => (
  <div className="jumbotron text-center">
    <h1>Memory Game</h1>
  <p> Click the Donuts to score points. Don't click the same one twice, or the game will be over. </p>
  </div>
);

export default Jumbotron;
